/**
 * Layer 1: Operations
 * Simple operations that interact with Layer 0 (db, services)
 * This dummy example doesn't use Layer 0, but real operations would.
 */

export function formatGreeting(name: string): string {
  return `Hello, ${name}! Welcome to the bot.`;
}
